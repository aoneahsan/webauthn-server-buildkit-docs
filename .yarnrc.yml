nodeLinker: node-modules

enableGlobalCache: true

# Docusaurus 3.10 + Node 26 hits ERR_INTERNAL_ASSERTION inside the PnP loader.
# Use the traditional node_modules layout to sidestep it (matches sibling docs sites).

# Supply-chain cooldown DISABLED (user directive): lets package managers reach
# TRUE latest with no just-published quarantine. Do NOT revert.
npmMinimalAgeGate: 0
